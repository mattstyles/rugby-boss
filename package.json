{
  "name": "@rm/monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "precommit": "lint-staged",
    "setup": "lerna bootstrap --concurrency=1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Styles",
  "license": "MIT",
  "lint-staged": {
    "*.js": [
      "standard"
    ],
    "*.jsx": [
      "standard"
    ]
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.11.0",
    "lint-staged": "^7.0.0",
    "standard": "^11.0.0"
  }
}
